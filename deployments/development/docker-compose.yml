version: '3.8'

x-var: &COMMON
  networks:
    internal:

networks:
  internal:
    # name: internal
    # external: true

services:
  helloworld:
    <<: *COMMON
    build: ../../build
    image: helloworld:1
    container_name: helloworld
    hostname: helloworld
    # ports:
    #   - 12345:12345
    # volumes:
    #   - .:/src
    # restart: always
    # tty: true
    environment:
      ENVIRONMENT: $ENVIRONMENT
    # entrypoint:
    #   - /sbin/tini
    #   - --
    # command:
    #   - tail
    #   - -f
    #   - /dev/null
