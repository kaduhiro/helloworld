version: '3.8'

x-var: &COMMON
  networks:
    internal:

networks:
  internal:
    # name: internal
    # external: true

services:
  helloworld:
    <<: *COMMON
    build: .
    image: helloworld:1
    container_name: helloworld
    hostname: helloworld
    # ports:
    #   - 12345:12345
    # volumes:
    #   - .:/src
    # environment:
    #   TERM: $TERM
    # restart: always
    # tty: true
    # entrypoint:
    #   - /sbin/tini
    #   - --
    # command:
    #   - tail
    #   - -f
    #   - /dev/null
